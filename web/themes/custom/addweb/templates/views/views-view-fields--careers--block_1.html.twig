{#
/**
 * @file
 * Default view template to display all the fields in a row.
 *
 * Available variables:
 * - view: The view in use.
 * - fields: A list of fields, each one contains:
 *   - content: The output of the field.
 *   - raw: The raw data for the field, if it exists. This is NOT output safe.
 *   - class: The safe class ID to use.
 *   - handler: The Views field handler controlling this field.
 *   - inline: Whether or not the field should be inline.
 *   - wrapper_element: An HTML element for a wrapper.
 *   - wrapper_attributes: List of attributes for wrapper element.
 *   - separator: An optional separator that may appear before a field.
 *   - label: The field's label text.
 *   - label_element: An HTML element for a label wrapper.
 *   - label_attributes: List of attributes for label wrapper.
 *   - label_suffix: Colon after the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 *   - has_label_colon: A boolean indicating whether to display a colon after
 *     the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 * - row: The raw result from the query, with all data it fetched.
 *
 * @see template_preprocess_views_view_fields()
 *
 * @ingroup themeable
 */
#}

<div class="careers-main" id="career-share-{{fields.id.content}}">
  <div class="careers-main-left">        
      <div class="careers-main-technology-image">       
        {% if fields.field_technology.content | length > 0 %}
          {{ fields.field_technology.content }}
        {% endif %}  
      </div>     
  </div>

  <div class="careers-main-right">
    <div class="careers-main-title">
      {% if fields.field_title.content | length > 0 %}
        {{ fields.field_title.content }}
      {% endif %}
    </div>

      <div class="social-share">        
        <div class="careers-share"> 
           <div class="careers-path-hide">
            {% if fields.id.content | length > 0 %}
              {% set share_path = base_url~'/careers#career-share-'~fields.id.content %}
              {% set share_text = fields.field_title.content %}
              {{ share_path }}
            {% endif %}
          </div>         
          <div class="plus social-icon">           
          </div>
          <div class="facebook social-icon">
            <a href="http://www.facebook.com/sharer.php?u={{ share_path }}" target="_blank" title="Share on Facebook">Facebook</a>
          </div>
          <div class="twitter social-icon">
            <a href="https://twitter.com/share?&text={{ share_text }}&url={{ share_path }}" target="_blank" data-count="none" title="Share on Twitter">Twitter</a>
          </div>
        </div>
      </div>

    <div class="careers-main-technology">
      {% if fields.field_technology_1.content | length > 0 %}
        {{ fields.field_technology_1.content }}
      {% endif %}
    </div>

    <div class="careers-main-technical-skill">
      {% if fields.field_technical_skill.content | length > 0 %}
        {{ fields.field_technical_skill.content }}
      {% endif %}
    </div>

    <div class="careers-main-full-body">
      {% if fields.field_body_1.content | length > 0 %}
        {{ fields.field_body_1.content }}
      {% endif %}
    </div>

    <div class="careers-main-body">
      {% if fields.field_body.content | length > 0 %}
        {{ fields.field_body.content }}
      {% endif %}      
    </div>

    <div class="careers-main-share">
      <div class="careers-path-hide">
        {% if fields.id.content | length > 0 %}
          {% set share_path = base_url~'/careers#career-share-'~fields.id.content %}
          {% set share_text = fields.field_title.content %}         
        {% endif %}
      </div>
      <div class="facebook social-icon">
        <a href="http://www.facebook.com/sharer.php?u={{ share_path }}" target="_blank" title="Share on Facebook">Facebook</a>
      </div>
      <div class="twitter social-icon">
        <a href="https://twitter.com/share?&text={{ share_text }}&url={{ share_path }}" target="_blank" data-count="none" title="Share on Twitter">Twitter</a>
      </div>
    </div>
  </div>
</div>