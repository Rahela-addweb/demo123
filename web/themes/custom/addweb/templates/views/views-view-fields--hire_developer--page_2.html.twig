{#
/**
 * @file
 * Default view template to display all the fields in a row.
 *
 * Available variables:
 * - view: The view in use.
 * - fields: A list of fields, each one contains:
 *   - content: The output of the field.
 *   - raw: The raw data for the field, if it exists. This is NOT output safe.
 *   - class: The safe class ID to use.
 *   - handler: The Views field handler controlling this field.
 *   - inline: Whether or not the field should be inline.
 *   - wrapper_element: An HTML element for a wrapper.
 *   - wrapper_attributes: List of attributes for wrapper element.
 *   - separator: An optional separator that may appear before a field.
 *   - label: The field's label text.
 *   - label_element: An HTML element for a label wrapper.
 *   - label_attributes: List of attributes for label wrapper.
 *   - label_suffix: Colon after the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 *   - has_label_colon: A boolean indicating whether to display a colon after
 *     the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 * - row: The raw result from the query, with all data it fetched.
 *
 * @see template_preprocess_views_view_fields()
 *
 * @ingroup themeable
*/
#} 
  <div class="hire-developer-list">
    <div class="hire-devloper-content">
      <div class="hire-developer-image"> 
        {% if fields.field_technology_image.content | length > 0 %}
          {{ fields.field_technology_image.content }}
        {% endif %}
      </div>

      <div class="hire-developer-body">
        {% if fields.description__value.content | length > 0 %}
          {{ fields.description__value.content }}
        {% endif %}
      </div>
      <div class="hire-developer-seo-title">
        {% if fields.field_hire_seo_title.content | length > 0 %}
          <h2>{{ fields.field_hire_seo_title.content }}</h2>
        {% endif %}
      </div>
      <div class="hire-developer-seo-description">
        {% if fields.field_hire_seo_description.content | length > 0 %}
          {{ fields.field_hire_seo_description.content }}
        {% endif %}
      </div>
    </div>
    <div class="quote-button">
        <a href="/contact">GET QUOTE NOW</a>
    </div>
    <div class="company-status-wrap">
      {% if fields.field_hours_of_experience.content | length > 0 %}
      <div class="hours-of-experience company-status-inner-wrap">
        <div class="title">{{ 'Hours of experience in the technology' }}</div>
        <div class="company-content">        
          {{ fields.field_hours_of_experience.content }}
          <span>Hours</span>
        </div>      
      </div>
      {% endif %}
      {% if fields.field_total_ticket_served.content | length > 0 %} 
      <div class="Total-ticket-served company-status-inner-wrap">
        <div class="title">{{ 'Total Ticket Served' }}</div>
        <div class="company-content">
          {{ fields.field_total_ticket_served.content }}
          <span>Tickets</span>
        </div>  
      </div>
      {% endif %}
      {% if fields.field_team_size_of_technology.content | length > 0 %}  
      <div class="Team-size-of-technology company-status-inner-wrap">
        <div class="title">{{ 'Team Size of Technology' }}</div>
        <div class="company-content">
          {{ fields.field_team_size_of_technology.content }}
          <span>Members</span>
        </div>
      </div> 
      {% endif %}
      {% if fields.field_total_project_delivered.content | length > 0 %}
      <div class="Total-project-delivered company-status-inner-wrap">
        <div class="title">{{ 'Total Project Delivered' }}</div>
        <div class="company-content">
          {{ fields.field_total_project_delivered.content }}
          <span>Projects</span>
        </div>
      </div> 
      {% endif %} 
    </div>
  </div>

